<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has '_' prefix like _AppliedTo, _UninitReason, ect...?
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Crypt' prefix like CryptEncode, CryptDecode, ect...?
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Alert' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Check' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Debug' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Expert' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Get' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Message' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Play' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Print' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Resource' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sleep' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Terminal' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tester' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Translate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Zero' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Period' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reset' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Set' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Array' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Assign' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Copy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Eye' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Identity' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ones' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Zeros' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Full' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tri' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Init' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Fill' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Random' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Manipulations' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Has' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Transposepy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Diag' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Col' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Concat' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Compare' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Flat' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Clip' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reshape' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Resize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Swap' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Split' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Hsplit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Vsplit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Arg' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sort' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Operations' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Products' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mat' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ge' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Power' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Dot' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Kron' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Inner' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Outer' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Corr' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cov' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Correlate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Convolve' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Transformations' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cholesky' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Eig' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'LU' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'LUP' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'QR' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'SVD' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Features' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rows' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cols' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Size' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Norm' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cond' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Det' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'SLog' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rank' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Trace' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Spectrum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Solutions' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Solve' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lst' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Inv' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'PInv' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Max' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Min' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ptp
' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Prod' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Percentile' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Quantile' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Median' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mean' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Average' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Std' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Var' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Linear' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Machine' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Activation' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Derivative' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Loss' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Regression' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Confusion' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Classification' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Precision' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Receiver' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Singular' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Eigen' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Inverse' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Least' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Factorization' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'PLU' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'LDL' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cholesky' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pseudo' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Polar' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Dynamic' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reduce' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reflect' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Char' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Color' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Double' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Enum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Integer' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Short' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Time' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Normalize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'String' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Math' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Struct' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Account' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Is' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Uninitialize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'MQL' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Symbol' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Digits' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Point' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'On' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Symbols' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Market' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Calendar' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Organizing' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Series' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bars' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Indicator' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'i' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Custom' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Chart' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Order' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Positions' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Position' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Orders' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'History' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Signal' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Socket' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Web' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Send' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Global' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'File' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Folder' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'DRAW' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Plot' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Object' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Objects' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Text' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Frame' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Parameter' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Event' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'CL' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Database' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'DX' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'initialize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'login' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'shutdown' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'version' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'last' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'account' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'terminal' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'symbols' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'symbol' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'market' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'copy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'orders' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'order' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'positions' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'history' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'ONNX' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Format' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Onnx' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Datastructures' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mathematics' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Statistics' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Statistical' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'C' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Composition' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Membership' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'A1' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'A2' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'C1' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'C2' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'A' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'B' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'B1' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'B2' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sigma' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'CP' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'D' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'CS' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'X1' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'X2' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'X3' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'X4' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'To' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'CZ' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'I' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Fuzzy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Conclusion' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Weight' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Not' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Term' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Conditions' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Op' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Condition' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Create' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Add' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Terms' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Values' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Functions' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mamdani' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Aggregation' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Calculate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Defuzzification' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Empty' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Implication' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Output' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Parse' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rules' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sugeno' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Open' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Buffer' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Execute' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Initialize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Kernel' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Shutdown' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Support' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Basic' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Prev' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Next' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Save' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Load' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Type' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Data' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Step' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Total' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Available' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Clear' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reserve' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Insert' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Update' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Shift' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Delete' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'At' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Search' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Delta' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Minimum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Maximum' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Free' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Detach' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Index' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Move' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Exchange' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Owner' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Left' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Right' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Balance' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Refresh' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Root' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Find' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Generic' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Equals' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Hash' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Count' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Contains' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Remove' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Try' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Last' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Value' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Parent' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Except' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Intersect' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Symmetric' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Union' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Overlaps' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Key' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Clone' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Capacity' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Trim' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Binary' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Reverse' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'List' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Previous' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Comparer' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Head' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'First' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Enqueue' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Dequeue' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Peek' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Files' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Push' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pop' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Handle' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Filename' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Flags' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Close' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tell' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Seek' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Flush' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Write' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Read' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Strings' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Str' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Len' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Append' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mid' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Replace' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Graphic' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Window' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Name' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Num' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Attach' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Price' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Style' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Width' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Background' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Selected' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Selectable' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Description' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tooltip' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Timeframes' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Levels' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Level' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Line' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ray' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Deviations' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Angle' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Channel' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Fibonacci' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Gann' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pips' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Downtrend' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Degree' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lines' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Scale' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ellipse' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Elliott' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Shape' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Arrow' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Arrows' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Control' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Anchor' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Font' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Back' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Border' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Corner' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'State' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bmp' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Date' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Arc' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pie' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Circle' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Destroy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Erase' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Height' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pixel' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Polygon' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Polyline' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rectangle' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Transparent' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Triangle' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Show' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'V' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Draw' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Allowed' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Legend' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Accumulative' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Descriptor' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Redraw' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Calc' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Gradient' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bar' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Filled' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Label' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has '3D' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ambient' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Input' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Light' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Projection' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Render' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'View' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mode' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Foreground' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Auto' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Fixed' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Points' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Visible' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Windows' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bring' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Indicators' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Navigate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Apply' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Screen' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'X' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Y' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Default' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Select' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Scientific' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Graph' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Histogram' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Steps' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Trend' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Gap' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Grid' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Curve' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Curves' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Marks' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Major' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Indent' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Base' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Offset' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Buffers' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Timeframe' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Status' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Method' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Volume' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ci' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Timeseries' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ma' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Deviation' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Applied' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Upper' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lower' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Main' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Plus' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Minus' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tenkan' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Kijun' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Senkou' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Chinkou' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sar' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ind' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Oscillators' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Fast' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Slow' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Cmo' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ema' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Kperiod' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Dperiod' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Slowing' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bill' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Jaw' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Teeth' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lips' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Param' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Trade' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Login' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Leverage' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Stopout' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Margin' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Limit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Credit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Profit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Equity' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Server' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Currency' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Company' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Info' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Spread' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ticks' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Stops' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Freeze' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bid' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ask' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Tick' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Contract' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lots' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bank' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Path' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Session' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ticket' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Magic' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Stop' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Take' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Comment' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Store' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Identifier' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Commission' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Deal' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Entry' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Commision' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Log' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Buy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sell' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Request' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Result' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Build' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Code' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'CPU' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Memory' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Disk' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Language' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Common' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Strategy' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Used' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Every' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'High' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Low' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Real' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Validation' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Start' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Deinit' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Processing' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Trailing' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Lot' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Prepare' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Wait' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'No' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Patterns' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'General' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ignore' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Invert' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Threshold' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Expiration' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Long' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Direction' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Percent' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Signals' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Modules' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Money' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Decrease' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Panels' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Top' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Bottom' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Month' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Day' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Days' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Sec' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Hour' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mon' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Year' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Controls' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rect' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Alignment' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Align' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Id' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Enable' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Disable' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Hide' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Activate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Deactivate' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Prop' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Mouse' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Drag' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Z' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Pressed' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Locking' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Curr' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'H' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Rebound' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Row' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Checked' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Caption' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Client' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Run' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Minimized' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Ini' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Minimize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Maximize' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Program' prefix.
</another_group>

<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights syntax (with different color) that has 'Subwin' prefix.
</another_group>






<another_group>
  is it possible to make the  
@mql5.tmLanguage.json
  automatically highlights normal syntax (with different color) that atleast starts withh a or A, b or B, c or C, etc...(single syntax that may have non-space or '_' connector) that is not written on 
@mql5.tmLanguage.json
  yet? (color a or A to z or Z with different colors individually)
</another_group>

